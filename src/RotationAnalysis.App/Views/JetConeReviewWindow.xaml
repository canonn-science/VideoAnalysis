<Window x:Class="RotationAnalysis.App.Views.JetConeReviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Icon="/Assets/canonn.ico"
        Title="Confirm Jet Cone Distance" Height="520" Width="900"
        WindowStartupLocation="CenterOwner" SizeToContent="Height">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Confirm the distance reading" FontSize="18" FontWeight="SemiBold"
                   Foreground="{StaticResource CanonnAccentBrush}"/>

        <TextBlock Grid.Row="1" Margin="0,4,0,0" TextWrapping="Wrap" FontSize="13"
                   Foreground="{StaticResource CanonnTextMutedBrush}"
                   Text="Frame captured at the moment the FSD warning overlay appeared. Check the value against the reticle readout below and correct it if needed."/>

        <Border Grid.Row="2" Background="Black" BorderBrush="{StaticResource CanonnBorderBrush}" BorderThickness="1"
                Margin="0,12,0,0" Padding="4" HorizontalAlignment="Center">
            <Image x:Name="ReticleImage" Stretch="Uniform" MaxHeight="480" MaxWidth="820"/>
        </Border>

        <Border Grid.Row="3" Background="Black" BorderBrush="{StaticResource CanonnBorderBrush}" BorderThickness="1"
                Margin="0,8,0,0" Padding="4" HorizontalAlignment="Center">
            <Image x:Name="BottomLeftImage" Stretch="Uniform" MaxHeight="180" MaxWidth="820"/>
        </Border>

        <Grid Grid.Row="4" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Distance (Ls):" VerticalAlignment="Center" FontWeight="SemiBold"
                       Foreground="{StaticResource CanonnTextBrightBrush}" Margin="0,0,10,0"/>
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBox x:Name="DistanceTextBox" Width="140" HorizontalAlignment="Left" FontSize="16"/>
                <TextBlock x:Name="SourceText" FontSize="11" Margin="0,4,0,0"
                           Foreground="{StaticResource CanonnTextMutedBrush}"/>
            </StackPanel>

            <Button Grid.Column="2" Content="Cancel" Padding="16,8" Margin="0,0,8,0" Click="CancelButton_Click"/>
            <Button Grid.Column="3" Content="Save" Padding="24,8" IsDefault="True"
                    Background="{StaticResource CanonnAccentBrush}" Foreground="White"
                    Click="SaveButton_Click"/>
        </Grid>
    </Grid>
</Window>
