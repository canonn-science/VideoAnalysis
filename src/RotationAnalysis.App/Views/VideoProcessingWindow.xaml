<Window x:Class="RotationAnalysis.App.Views.VideoProcessingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Icon="/Assets/canonn.ico"
        Title="Analyzing Video…" Height="700" Width="1000"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize">
    <Grid Background="Black">
        <Image x:Name="CurrentFrameImage" Stretch="Uniform"/>

        <!-- Overlay panel across the bottom portion of the frame -->
        <Border VerticalAlignment="Bottom" Background="#CC1C1C1C" Padding="20,14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="StatusText" Grid.Column="0" Text="Opening video…" TextWrapping="Wrap"
                               FontSize="15" Foreground="{StaticResource CanonnTextBrightBrush}" VerticalAlignment="Center"/>
                    <TextBlock x:Name="FrameCounterText" Grid.Column="1" Margin="12,0,0,0" FontSize="15"
                               Foreground="{StaticResource CanonnAccentBrush}" VerticalAlignment="Center"/>
                </Grid>

                <ProgressBar x:Name="ProgressBarControl" Grid.Row="1" Margin="0,10,0,0" Height="8" Minimum="0" Maximum="100"
                             Foreground="{StaticResource CanonnAccentBrush}"/>

                <Button Grid.Row="2" Content="Cancel" HorizontalAlignment="Right" Margin="0,14,0,0" Padding="16,8"
                        Click="CancelButton_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>
